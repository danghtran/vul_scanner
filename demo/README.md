# Demo scan outputs

Sample results from authorized test targets. Regenerate with:

```bash
python main.py --target scanme.nmap.org --ports 22 80 --internet-facing
```

Files are named `demo/<hostname>_scan.json` and `demo/<hostname>_report.txt`.
